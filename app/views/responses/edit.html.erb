<!-- Validation failure messages -->
<% if @response.errors.any? %>
  <% @response.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="row">
  <div class="col-md-12">
    <div class="jumbotron bg-primary text-light">
      <h1 class="display-4">Update text</h1>
       <form action="/update_response/<%= @response.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">
      <input name="question_id" type="hidden" value="<%=@response.question_id%>">
      <input type="hidden" id="time" name="time" placeholder="Time" class="form-control" value="<%= @response.time %>">
      <!-- Label and input for time -->
            <!-- Label and input for response_text -->
      <div class="form-group">
        <textarea class="form-control form-control-lg" name="response_text" rows="3"><%= @response.response_text %></textarea>
      </div>

      <button class="btn btn-light">
        Update 
      </button>
      

    </form>
          <form action="/delete_response/<%=@response.id%>">
        <button class="btn btn-light">Delete</button>
      </form>
      or
      <a href="/" class="badge badge-light">Cancel</a>
      
      
    </div>
    
    
    
    
   
  </div>
</div>
